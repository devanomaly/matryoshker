"""Lending app: loans, reservations and overdue handling.

This app has no HTTP surface of its own. Everything it exposes is reached
through the catalog views or through the scheduled overdue job.
"""

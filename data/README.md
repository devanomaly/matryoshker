# data/

One subdirectory per mapped repository: `data/<repo>/usecases.json` (the versioned
use-case registry; see `schemas/usecases.schema.json` and `docs/data-contract.md`).

The bundled fixture keeps its registry next to the code it describes, at
`examples/sample-drf/usecases.json`.

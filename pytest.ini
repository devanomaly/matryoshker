# Makes a bare `python -m pytest -q` at the repository root run the project suite.
# examples/ holds a sample target repository whose tests are documentation, not
# tests of Matryoshker: they import the sample package and are not runnable here.
[pytest]
testpaths = tests
norecursedirs = examples extractor .git out node_modules
